"""Metrics, subject-level intervals and the cross-arm comparison.

Every number the project reports flows through `metrics`, and every interval
through `intervals`, so no arm can drift on a definition.
"""
